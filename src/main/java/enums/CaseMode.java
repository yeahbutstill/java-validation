package enums;

/**
 * Check Case Constrain
 * misalnya kita akan membuat sebuah constrain validation yang digunakan untuk melakukan
 * pengecekan case sebuah string
 * agar dinamis, kita akan tambahkan method mode() nya di constrain, yang bisa dipilih apakah
 * harus UPPER atau LOWER
 */
public enum CaseMode {
    UPPER, LOWER
}
